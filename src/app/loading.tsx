const Loading = () => {
  return (
    <div className="w-full min-h-screen flex place-content-center">
      <h1 className="text-4xl font-semibold text-zinc-800">Loading...</h1>
    </div>
  );
};

export default Loading;
